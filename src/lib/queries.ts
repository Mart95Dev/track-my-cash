// Ce fichier est maintenu pour backward compatibility.
// Tout le code a été splitté dans src/lib/queries/*.ts
// Les imports existants comme `import { X } from '@/lib/queries'` fonctionnent toujours.

export * from "./queries/index";
